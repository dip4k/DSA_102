# Stacks Traversal README

Use this folder for LIFO reasoning, monotonic constraints, parsing, and stack-state problems.

Canonical file:
- `stack_mastery_guide_L1-L6_lenient.md` : the single merged guide for stack concepts, visuals, drills, and problem ladders

Coverage tags:
- `Must` : basic stack traversal, parentheses, recursion simulation, monotonic stack
- `Should` : min-stack, two-stack constructions, contribution/range thinking
- `Optional` : deeper abstraction and parsing workflows

Recommended flow:
1. Read the mastery guide level by level.
2. Use the merged timed practice ladder at the end.
3. Treat the former drill, visual, and problem-set files as folded into the canonical guide.