# Strings Traversal README

Use this folder for string cursor movement, window control, parsing, and matching patterns.

Canonical file:
- `string_traversal_mastery_all_levels.md` : the single merged guide for traversal, windows, advanced matching, Unicode, drills, and performance

Coverage tags:
- `Must` : linear scans, reverse scans, two pointers, windows, parsing basics
- `Should` : KMP, prefix tables, repeated-pattern reasoning
- `Optional` : Unicode-specific and performance-focused deep dives

Recommended flow:
1. Read the main mastery guide from Level 1 to Level 9.
2. Use the merged advanced appendix and drill ladder near the end.
3. Treat former matching, playbook, and drill files as merged into the canonical guide.