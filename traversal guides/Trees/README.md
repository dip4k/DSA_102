# Trees Traversal README

Use this folder for tree-walking mastery across DFS, BFS, views, and advanced traversal variants.

Canonical file:
- `Tree_Traversal_Mastery_Unified_with_Appendix.md` : the single merged guide for tree traversal concepts, appendix modules, practice ladder, Morris notes, and drill guidance

Coverage tags:
- `Must` : preorder, inorder, postorder, level-order, subtree reasoning
- `Should` : views, boundary traversal, path-state DFS, N-ary variants
- `Optional` : Morris traversal, binary-only supplements, extra drill packs

Recommended flow:
1. Read the unified guide from the mental model through Level 5.
2. Use the merged practice ladder at the end.
3. Treat former visual, Morris, drill, and problem-set files as folded into the canonical guide.